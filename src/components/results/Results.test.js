import { default as Results } from '.';
import { shallow } from 'enzyme';

describe('Results', () => {
    let component
    beforeEach(() => {
     
    });

    test("it should render", () => {
        
    });

    test("it shows repoName", () => {
        
    });

    test("it shows the Fork Counts per repo", () => {
        
    });
    test("it shows the Stargazer Counts per repo", () => {
        
    });
})
